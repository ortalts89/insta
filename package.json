{
  "name": "myproject",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "NODE_ENV=development lerna bootstrap && npm run build",
    "start": "cd apps/backend && npm start",
    "build": "lerna run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lerna": "^4.0.0"
  }
}
